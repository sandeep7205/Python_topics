- In Python, if a function doesn’t explicitly use return, it automatically returns None.

- Star operator ("*") behaves differently depending on data types (int × int = arithmetic, str × int = repetition).

- Always validate data types before performing operations. Python won’t always throw an error — sometimes it’ll “do something else” that looks correct but isn’t what you meant.