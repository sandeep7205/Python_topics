# Define two lists of equal length
list1 = [1, 2, 3, 4]
list2 = ['a', 'b', 'c', 'd']

# Use the zip function to combine the lists into a list of tuples
combined_list = list(zip(list1, list2))

# Print out the combined list of tuples
print(combined_list)
